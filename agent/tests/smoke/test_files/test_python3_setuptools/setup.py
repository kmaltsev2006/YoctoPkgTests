from setuptools import setup, find_packages

setup(
    name='yocto-test-package',
    version='0.1.0',
    packages=find_packages(),
    description='Minimal test package for setuptools verification',
)